package com.subex.automation.helpers.util;

import com.subex.automation.helpers.component.FileHelper;
import com.subex.automation.helpers.selenium.AcceptanceTest;

//import atu.testrecorder.ATUTestRecorder;

public class VideoHelper extends AcceptanceTest {

	/*
	 * private ATUTestRecorder recorder = null;
	 * 
	 * public void startRecording(String videoDirectory, String fileName) throws
	 * Exception { try { if (!FileHelper.checkDirectoryExists(videoDirectory)) { int
	 * index = videoDirectory.lastIndexOf("\\");
	 * FileHelper.makeDirectory(videoDirectory.substring(0, index),
	 * videoDirectory.substring(index)); }
	 * 
	 * recorder = new ATUTestRecorder(videoDirectory, fileName, false);
	 * recorder.start(); } catch (Exception e) { FailureHelper.setErrorMessage(e);
	 * throw e; } }
	 * 
	 * public void stopRecording() throws Exception { try { recorder.stop(); } catch
	 * (Exception e) { FailureHelper.setErrorMessage(e); throw e; } }
	 */
}